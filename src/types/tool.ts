/**
 * Tool types — Re-exports from src/tools/Tool.ts for convenience.
 *
 * The canonical definitions live in src/tools/Tool.ts.
 * This file provides re-exports so consumers can import from either location.
 */

export type { Tool, ToolContext, ToolResult } from "../tools/Tool.js";
